doctrine:
    dbal:
        # Datei-basierte SQLite Testdatenbank, stabil über mehrere Kernel/Connections
        url: 'sqlite:///%kernel.project_dir%/var/test.db'
    
    orm:
        auto_mapping: true
        mappings:
            App:
                is_bundle: false
                type: attribute
                dir: '%kernel.project_dir%/src/Entity'
                prefix: 'App\Entity'
                alias: App 